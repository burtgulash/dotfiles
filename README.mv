# My Dotfiles

## Symlink technique:
Keep all dotfiles in ~/dotfiles directory and keep this directory versioned by
git. Symlink from ~/ to all dotfiles in this directory.

eg.
+ ~/.bashrc -> ~/dotfiles/.bashrc
+ ~/.vimrc -> ~/dotfiles/.vimrc
+ ~/.zshrc -> ~/dotfiles/.zshrc
